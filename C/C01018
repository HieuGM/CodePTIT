#include<stdio.h>
#include<math.h>
int check(int n)
{
	int a = sqrt(n);
	if (a*a == n) return 1;
	return 0;
}
int main()
{
	int t;
	scanf("%d", &t);
	while (t--)
	{
		int n;
		scanf("%d", &n);
		if (check(n)) printf("YES\n");
		else printf("NO\n");
	}
}
